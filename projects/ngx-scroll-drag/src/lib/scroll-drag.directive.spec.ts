import { ScrollDragDirective } from './scroll-drag.directive';

describe('ScrollDragDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollDragDirective({},{});
    expect(directive).toBeTruthy();
  });
});
